//////////////////////////////////////////////////////////////////
// Check Player Skill & Upgrade as Needed Using Correct Method.
export function Check(ns, skillName, level){  
  let currentWork = ns.singularity.getCurrentWork()    
  let sName = skillName || "strength";
  
}